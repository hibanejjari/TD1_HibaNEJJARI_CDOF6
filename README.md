# TD1_HibaNEJJARI_CDOF6

To-Do List Application
A simple command-line application to manage your tasks efficiently. This project allows users to view, add, delete, and mark tasks as completed in an easy-to-use interactive interface.

Features
View Tasks: Display all tasks with their status (completed or not).
Add Task: Add new tasks with a description.
Delete Task: Remove tasks based on their number in the list.
Complete Task: Mark specific tasks as completed.
User-Friendly Menu: Intuitive menu to navigate through the application options.
How to Run the Project
Clone the repository or download the script file:

bash
Copier le code
git clone <repository_url>
cd <project_directory>
Ensure Python 3.x is installed on your system. You can check by running:

bash
Copier le code
python --version
Execute the script:

bash
Copier le code
python todo_list_app.py
Follow the on-screen instructions to manage your tasks.

How to Contribute
We welcome contributions from the community! Here's how you can contribute:

Fork the Repository:

Click on the "Fork" button in the repository's GitHub page.
Clone the Forked Repository:

bash
Copier le code
git clone <your_forked_repository_url>
cd <forked_project_directory>
Make Changes:

Add your improvements or bug fixes to the code.
Test Your Changes:

Ensure your modifications don't break existing functionality.
Commit and Push:

bash
Copier le code
git add .
git commit -m "Your descriptive commit message"
git push origin main
Create a Pull Request:

Go to the original repository and open a pull request from your fork.
Future Enhancements
Add task prioritization levels.
Allow task editing.
Save tasks to a file for persistence.
Add support for task categories.
